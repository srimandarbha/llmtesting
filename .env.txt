# =============================================================================
# SRE Incident Agent — Environment Variables (LOCAL DEV)
# =============================================================================

# ─── Database (local PostgreSQL with pgvector) ──────────────────────────────
DB_HOST=localhost
DB_PORT=5432
DB_NAME=rhokp
DB_USER=postgres
DB_PASSWORD=postgres

# ─── LLM API ─────────────────────────────────────────────────────────────────
# Local llama.cpp server (default) — compatible with OpenAI API format
# LLM_API_URL=http://host.docker.internal:8080/v1/chat/completions
# For OpenRouter:
#LLM_API_URL=https://openrouter.ai/api/v1/chat/completions
#LLM_API_KEY=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
#LLM_MODEL=openrouter/free
LLM_API_URL=http://host.docker.internal:8080/v1/chat/completions
LLM_API_KEY=sk-local
LLM_MODEL=phi4

# ─── Observability APIs ───────────────────────────────────────────────────────
PROMETHEUS_URL=http://127.0.0.1:9090
SPLUNK_URL=http://127.0.0.1:8088
SPLUNK_CONTROLLER_NAME=argocd-application-controller

# ─── AWX / Ansible Tower ──────────────────────────────────────────────────────
AWX_BASE_URL=http://localhost:8052
AWX_API_TOKEN=dev-token
# Mock mode ON for local dev — no real AWX needed
USE_MOCK_AWX=true

# ─── Celery (uses PostgreSQL — no Redis needed) ──────────────────────────────
# These are auto-derived from DB_* vars if not set. Override only if needed.
# CELERY_BROKER_URL=db+postgresql://sreagent:changeme@localhost:5432/rhokp
# CELERY_RESULT_BACKEND=db+postgresql://sreagent:changeme@localhost:5432/rhokp

# ─── Auth / API Key ───────────────────────────────────────────────────────────
API_KEY=dev-api-key-change-in-prod

# ─── PagerDuty ────────────────────────────────────────────────────────────────
# Leave empty to skip PagerDuty escalation (logs warning instead)
PAGERDUTY_API_KEY=
PAGERDUTY_ESCALATION_POLICY_ID=

# ─── Safety Controls ─────────────────────────────────────────────────────────
BLAST_RADIUS_CAP=5
DEDUP_WINDOW_MINUTES=10

# ─── Embedding Model ─────────────────────────────────────────────────────────
EMBED_MODEL_NAME=all-MiniLM-L6-v2
